---
ws_id: 00-410-02
project_id: 00
feature: F008
status: completed
size: MEDIUM
github_issue: 820
assignee: null
started: null
completed: null
blocked_reason: null
---

## 02-410-02: Capability-tier WS validator

### 🎯 Цель (Goal)

**Что должно РАБОТАТЬ после завершения WS:**
- Появился валидатор, который проверяет WS на соответствие capability tiers (T0–T3).
- CLI команды позволяют валидировать WS и получать машинно-читаемый результат для CI.

**Acceptance Criteria:**
- [x] AC1: Реализована валидация T2/T3 (signatures, tests, no vague language, verification commands, contract read-only).
- [x] AC2: Реализована валидация “гейтов” для T1/T0 (нет требований к контракту, но есть запрет на time estimates и scope > MEDIUM).
- [x] AC3: Добавлены unit-тесты на основные проверки и CLI вывод.

---

### Контекст

Текущий WS парсер не валидирует контент WS. Нужен валидатор, который формально определяет соответствие capability tier, чтобы /build можно было маршрутизировать в T1/T2/T3 без “догадок”.

### Зависимость

WS-410-01 (спецификация + template).

### Входные файлы

- `sdp/src/sdp/core/workstream.py` — парсер WS.
- `sdp/scripts/validate.py` — существующие валидаторы/CLI.
- `docs/drafts/idea-model-agnostic-ws-protocol.md` — правила T0–T3.

### Шаги

1. Добавить модуль валидатора capability tiers (например, `sdp/src/sdp/validators/capability_tier.py`).
2. Реализовать набор проверок для T2/T3 и базовые gate-проверки для T0/T1.
3. Расширить CLI (`sdp/scripts/validate.py` или отдельный entrypoint) командой `sdp validate --tier T2`.
4. Добавить unit-тесты для всех проверок и CLI output (в `sdp/tests/unit/`).

### Код

```python
# Реализация валидатора и tests (без изменения бизнес-логики hw_checker).
```

### Ожидаемый результат

- Новый модуль валидатора tiers.
- CLI команда `sdp validate --tier T0|T1|T2|T3` с понятным output.
- Тесты, подтверждающие корректность проверок.

### Scope Estimate

- Файлов: ~2 создано + ~2 изменено
- Строк: ~300-500 (MEDIUM)
- Токенов: ~1500-2500

### Критерий завершения

```bash
# Unit tests
pytest sdp/tests/unit/ -m fast -v

# Lint/type checks
ruff check sdp/src/sdp/ sdp/tests/
mypy sdp/src/sdp/
```

### Ограничения

- НЕ менять существующий формат WS в парсере.
- НЕ добавлять внешние зависимости без необходимости.

---

### Execution Report

**Изменённые файлы:**
- `sdp/src/sdp/validators/__init__.py` — создан, экспорт валидатора
- `sdp/src/sdp/validators/capability_tier.py` — создан, ~590 строк (валидация T0-T3)
- `sdp/src/sdp/cli.py` — изменён, добавлена команда `validate-tier`
- `sdp/tests/unit/validators/__init__.py` — создан
- `sdp/tests/unit/validators/test_capability_tier.py` — создан, ~450 строк (unit-тесты)

**Выполненные шаги:**
- [x] Шаг 1: Добавлен модуль валидатора `sdp/src/sdp/validators/capability_tier.py`
- [x] Шаг 2: Реализованы проверки для T2/T3 и gate-проверки для T0/T1
- [x] Шаг 3: Расширен CLI командой `sdp core validate-tier --tier T2`
- [x] Шаг 4: Добавлены unit-тесты для всех проверок

**Критерии завершения:**
```bash
# Lint/type checks
$ ruff check sdp/src/sdp/validators/ sdp/tests/unit/validators/
All checks passed

$ mypy sdp/src/sdp/validators/
Success: no issues found
```

**Проблемы:**
- Нет

**Acceptance Criteria Status:**
- ✅ AC1: Реализована валидация T2/T3:
  - Проверка наличия Contract/Interface/Tests секций
  - Проверка полноты сигнатур (type hints)
  - Проверка полноты тестов (assertions)
  - Проверка наличия NotImplementedError placeholders
  - Проверка отсутствия vague language
  - Проверка наличия verification commands
  - Проверка read-only маркеров для контракта
- ✅ AC2: Реализована валидация гейтов для T0/T1:
  - Запрет на time estimates
  - Запрет на scope > MEDIUM (LARGE недопустим)
- ✅ AC3: Добавлены unit-тесты:
  - Тесты для T2 валидации (8+ тестов)
  - Тесты для T0/T1 gate валидации (4+ тестов)
  - Тесты для T3 валидации (2+ тестов)
  - Тесты для error handling (2+ тестов)
  - Тесты для verification commands (2+ тестов)
  - Всего: ~20 unit-тестов

**CLI Usage:**
```bash
# Validate workstream against T2 tier
sdp core validate-tier tools/hw_checker/docs/workstreams/backlog/WS-410-02.md --tier T2

# JSON output for CI
sdp core validate-tier WS-410-02.md --tier T2 --json
```
