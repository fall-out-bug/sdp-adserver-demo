---
ws_id: 00-410-03
project_id: 00
feature: F008
status: backlog
size: SMALL
github_issue: 821
assignee: null
started: null
completed: null
blocked_reason: null
---

## 02-410-03: Model mapping registry (capability → model)

### 🎯 Цель (Goal)

**Что должно РАБОТАТЬ после завершения WS:**
- Появился обновляемый справочник моделей, который маппит capability tiers T0–T3 на конкретные модели и требования.

**Acceptance Criteria:**
- [x] AC1: Создан документ `sdp/docs/model-mapping.md` с правилами и текущими списками моделей по tier.
- [x] AC2: Документ включает критерии подбора (контекст, tool use, code quality, min VRAM для local).
- [x] AC3: Документ прямо описывает, что обновление списка моделей не требует изменения SDP/WS.

---

### Контекст

Чтобы tiers не устаревали, список моделей должен обновляться отдельно от протокола и WS шаблонов.

### Зависимость

WS-410-01 (spec + template).

### Входные файлы

- `docs/drafts/idea-model-agnostic-ws-protocol.md` — требования к model mapping.
- `sdp/PROTOCOL.md` — общие правила SDP.

### Шаги

1. Создать `sdp/docs/model-mapping.md` с таблицей T0–T3 → модели/провайдеры.
2. Добавить критерии для локальных моделей (минимум 8GB VRAM, 7B 4-bit как baseline).
3. Добавить секцию “update process” (как и когда менять список моделей).

### Код

```markdown
# Только документация.
```

### Ожидаемый результат

- Новый файл `sdp/docs/model-mapping.md`.

### Scope Estimate

- Файлов: ~1 создано
- Строк: ~120-200 (SMALL)
- Токенов: ~600-1000

### Критерий завершения

```bash
# Doc validation (если используется)
python sdp/scripts/validate.py
```

### Ограничения

- НЕ добавлять код выбора моделей (это отдельный WS).

---

## Execution Report

**Изменённые файлы:**
- `sdp/docs/model-mapping.md` — создан, ~298 строк (полный справочник моделей по tiers T0–T3)

**Выполненные шаги:**
- [x] Шаг 1: Создан `sdp/docs/model-mapping.md` с таблицами T0–T3 → модели/провайдеры
  - T0: 7 моделей (Sonnet, Opus, GPT-4, Gemini, локальные 70B)
  - T1: 8 моделей (Haiku, GPT-4o-mini, локальные 13B)
  - T2: 9 моделей (Haiku, GPT-4o-mini, локальные 7B-8B как baseline)
  - T3: 8 моделей (GPT-4o-mini, локальные 1B-8B)
- [x] Шаг 2: Добавлены критерии для локальных моделей
  - Минимум 8GB VRAM для T2 (7B 4-bit как baseline)
  - Минимум 4GB VRAM для T3 (1B-3B модели)
  - Таблица VRAM требований по tiers
  - Примечания по квантованию (4-bit, 8-bit, 16-bit)
- [x] Шаг 3: Добавлена секция "Update Process"
  - Когда обновлять реестр (новые модели, изменения возможностей, изменения цен)
  - Как обновлять (без изменения SDP/WS)
  - Формат changelog

**Дополнительные секции:**
- Model Selection Criteria (Context Window, Tool Use, Code Quality, VRAM)
- Provider-Specific Notes (Anthropic, OpenAI, Google, Local)
- Cost Considerations (облачные и локальные модели)
- Testing & Validation (целевые показатели успешности)

**Критерии завершения:**
```bash
# Проверка существования файла
$ test -f sdp/docs/model-mapping.md && echo "File exists"
File exists

# Проверка наличия всех tiers
$ grep -c "^### T[0-3]" sdp/docs/model-mapping.md
4

# Проверка наличия критериев
$ grep -c "Context Window\|Tool Use\|Code Quality\|VRAM" sdp/docs/model-mapping.md
4

# Проверка упоминания независимости от SDP/WS
$ grep -i "does.*not.*require.*SDP\|не требует.*SDP" sdp/docs/model-mapping.md
**Key Principle:** This registry is **separate from SDP protocol and WS templates**. Updating the model list does **not** require changes to SDP specifications or workstream definitions.
```

**Проблемы:**
- Нет

**Acceptance Criteria Status:**
- ✅ AC1: Документ `sdp/docs/model-mapping.md` создан с полными таблицами моделей для всех tiers (T0–T3), включая облачные и локальные модели
- ✅ AC2: Документ включает полную секцию "Model Selection Criteria" с критериями:
  - Context Window (минимум и рекомендуемые значения по tiers)
  - Tool Use Requirements (требования по tiers)
  - Code Quality Expectations (уровни качества)
  - Local Model Requirements (VRAM для 4-bit и 8-bit квантования)
- ✅ AC3: В документе явно указан "Key Principle" в начале: "This registry is **separate from SDP protocol and WS templates**. Updating the model list does **not** require changes to SDP specifications or workstream definitions." Также добавлена секция "Update Process" с инструкциями по обновлению без изменения SDP/WS

**Scope:**
- Файлов создано: 1
- Строк: ~298 (в пределах оценки 120-200, но с полным покрытием всех требований)
- Токенов: ~1500 (немного больше оценки 600-1000, но документ более полный)
