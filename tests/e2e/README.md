# E2E Тесты с Playwright

Автоматизированное тестирование UI и API через браузерную автоматизацию.

## 🚀 Быстрый старт

```bash
# Установить зависимости (уже выполнено)
npm install

# Запустить все сервисы
docker compose up -d

# Запустить E2E тесты
npm run test:e2e

# Запустить с UI
npm run test:e2e:ui

# Запустить в headed режиме (с видимым браузером)
npm run test:e2e:headed

# Просмотреть отчет
npm run test:e2e:report
```

## 📁 Структура

```
tests/e2e/
├── playwright/
│   ├── demo-website.spec.ts    # Тесты UI демо-сайта
│   ├── api.spec.ts              # Тесты API интеграции
│   ├── visual.spec.ts           # Визуальная регрессия
│   └── helpers.ts              # Helper функции
└── README.md
```

## 🧪 Тесты

### demo-website.spec.ts
Тестирование UI демо-сайта:
- Главная страница
- Demo страница с баннерами
- Страница форматов
- Адаптивность

### api.spec.ts
Тестирование API:
- GET /api/v1/demo/slots
- GET /api/v1/demo/slots/:slot_id/banner
- CORS и ошибки

### visual.spec.ts
Визуальная регрессия:
- Скриншоты страниц
- Проверка размеров баннеров
- Адаптивность

## 🔧 Конфигурация

`playwright.config.js`:
- **baseURL**: http://localhost:3000 (переопределяется через BASE_URL)
- **API_BASE_URL**: http://localhost:8080 (для API тестов)
- **browser**: Chromium (headless по умолчанию)

## 📊 Отчеты

После запуска тестов генерируется HTML отчет:
```bash
npx playwright show-report
```

Отчет сохраняется в `playwright-report/`.

## 🔬 Тестирование в CI

```bash
# Запуск с параллелизацией
CI=true npm run test:e2e

# Сгенерировать отчет для CI
CI=true npm run test:e2e:report
```
